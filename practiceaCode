You are a Senior Test Automation Architect specializing in Playwright, JavaScript, SOLID principles and enterprise automation frameworks.

Analyze ONLY the currently opened files.

Current architecture:

- The spec creates the testData object manually.
- The spec reads JSON using baseTest.jsonUtils.
- The spec passes the entire testData object to the helper.
- The helper executes the business workflow.

Problem:

The spec has too many responsibilities:
1. Reading JSON.
2. Building testData.
3. Creating the helper.
4. Executing the business workflow.

The spec should only describe WHAT business scenario is being executed.

Refactor the framework as follows.

---------------------------------------
Step 1
---------------------------------------

Modify the helper so that it accepts:

- app
- workerCredentials
- assertions
- commonUtilities
- commonActions
- testContext
- testInfo
- page

instead of

- testData

---------------------------------------
Step 2
---------------------------------------

Modify the helper method

createILCTransaction()

to

createILCTransaction(testCaseName)

Example

await helper.createILCTransaction("IMP_Issue_001_1");

---------------------------------------
Step 3
---------------------------------------

Inside the helper:

Move the entire testData creation logic from the spec into the helper.

The helper should internally:

- Read JSON using the existing baseTest.jsonUtils
- Use the existing dynamicDataPath.js constants
- Merge workerCredentials
- Build the same testData object
- Continue using the existing workflow

Do NOT change any business logic.

---------------------------------------
Step 4
---------------------------------------

Refactor ONLY this spec.

The final spec should look similar to:

test("IMP_Issue_001_1", async ({ workerCredentials, app, page }, testInfo) => {

    const helper = new ILC_Issuance_Create_TestHelper({
        app,
        workerCredentials,
        assertions: Assertions,
        commonUtilities: CommonUtilities,
        commonActions: CommonActions,
        testContext: test,
        testInfo,
        page
    });

    await helper.createILCTransaction("IMP_Issue_001_1");

});

---------------------------------------
Requirements
---------------------------------------

- Do NOT modify any Page Object.
- Do NOT change Playwright fixtures.
- Do NOT change business logic.
- Do NOT change assertions.
- Do NOT change reporting.
- Do NOT change screenshots.
- Do NOT change workflow execution.
- Preserve existing behaviour.
- Remove duplicated JSON loading from the spec.
- Remove duplicated testData creation from the spec.
- Keep the helper responsible for preparing its own testData.

Finally explain:

1. What responsibilities moved from the spec to the helper.
2. Why this follows the Single Responsibility Principle.
3. Why future test cases become shorter.
4. Why maintenance becomes easier.
