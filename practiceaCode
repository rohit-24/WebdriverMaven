Here's the curl (Postman-importable) using what you've confirmed so far:

**REST — check MBeans (needs WC_adminhost_secure port, click that row to get it)**
```bash
curl -k -u wasadmin:yourpassword \
  "https://wcsduv0a40.onprem.cloud.test.group:<WC_adminhost_secure_port>/IBMJMXConnectorREST/mbeans"
```

**To import into Postman directly:**
1. Postman → Import → Raw text → paste the curl above
2. Under **Authorization** tab, set type = Basic Auth, fill username/password (Postman will handle the `-u` conversion automatically)
3. Under **Settings** (for this request) → turn **SSL certificate verification OFF** (equivalent to `-k`, since your console showed "Not secure"/self-signed cert)

**Check a specific server's state (once you have the port), example MBean query:**
```bash
curl -k -u wasadmin:yourpassword \
  "https://wcsduv0a40.onprem.cloud.test.group:<port>/IBMJMXConnectorREST/mbeans?objectName=WebSphere:type=Server,name=TIPlus28AsyncZone01ServerDevtst01_JVM1,*"
```

**If REST auth fails / connection refused**, fall back to testing the SOAP port is even listening (won't give app data, just confirms reachability):
```bash
curl -k -v "https://wcsduv0a40.onprem.cloud.test.group:10320"
```

Go grab that `WC_adminhost_secure` port number (click the row like you did for SOAP_CONNECTOR_ADDRESS) and send it over — I'll fill in the exact working curl once we have it.
