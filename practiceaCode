Refactor this helper class by extracting the major business steps of `createILCTransaction()` into private helper methods.

Requirements:
- Do not change any business logic or execution order.
- Do not modify any assertions or validations.
- Do not change method signatures used by tests.
- Keep the existing constructor unchanged.
- Do not introduce new classes.
- Do not use destructuring or perform any unrelated refactoring.

The goal is to make `createILCTransaction()` read like a high-level business workflow.

Example structure:

async createILCTransaction() {
    await this.initializeTransaction();
    await this.loginToLots();
    await this.launchImportLetterOfCredit();
    await this.fillGeneralDetails();
    await this.fillPartyDetails();
    await this.fillAmountDetails();
    await this.submitTransaction();
    await this.verifyTransaction();
}

Move the existing code into appropriately named helper methods without changing any functionality.

Each helper method should contain one logical business operation only.

Return the complete refactored helper class.
