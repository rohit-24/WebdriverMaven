def get_prompt(cypress_test: str) -> str:
    return f"""
I'm working on converting a Cypress test module to Playwright using TypeScript, Cucumber, and the Page Object Model (POM).

Please generate a **complete Playwright Page Object class and corresponding Step Definition file** based on:

**Cypress Test Snippets**:
{cypress_test}

Requirements:

1. **Playwright Page Object**
- Full implementation, not skeletal
- Use `getByTestId` where possible
- All selectors defined as const at class level
- Follow BasePage pattern
- Method names should reflect Cypress actions

2. **Step Definition File**
- Full Cucumber-style step bindings
- Use `ScenarioWorld` pattern for world object
- Delegate all logic to Page Object methods

Output two sections:
- === PageObject ===
- === StepDefinition ===
"""
