You are a Senior Playwright Automation Architect.

Analyze ONLY this helper file.

The helper has already been refactored to use the grouped `app` object.

Currently the helper still creates local aliases such as:

const lotsLoginPage = app.lots.login;
const lotsDashboardPage = app.lots.dashboard;
const tiPlusLoginPage = app.tiPlus.login;

These aliases are unnecessary.

Refactor the helper to use the `app` object directly throughout the class.

For example:

Replace

lotsLoginPage.launchLotsApp()

with

app.lots.login.launchLotsApp()

Replace

lotsDashboardPage.launchImportLetterOfCredit()

with

app.lots.dashboard.launchImportLetterOfCredit()

Requirements:

- Do NOT change business logic.
- Do NOT rename methods.
- Do NOT modify Page Objects.
- Do NOT modify the spec.
- Only remove unnecessary local alias variables.
- Keep functionality identical.
- Explain every change.
