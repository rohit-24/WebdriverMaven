# utils/logger.py

import logging

logging.basicConfig(
    level=logging.DEBUG,  # 🔁 Change to DEBUG to see all URL logs
    format="%(asctime)s — %(levelname)s — %(message)s"
)

logger = logging.getLogger(__name__)
