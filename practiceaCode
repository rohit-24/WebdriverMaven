import re

def extract_cypress_test(file_path: str) -> list[str]:
    with open(file_path, 'r', encoding='utf-8') as f:
        content = f.read()

    # Split by semicolon assuming 1 Cypress action per line
    steps = [line.strip() for line in content.split(";") if line.strip()]
    return steps
