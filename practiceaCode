import re

def main(playwright_code_raw: str) -> dict:
    code = playwright_code_raw.strip()
    
    # Remove markdown code fences if present
    code = re.sub(r'^```[\w]*\n?', '', code)
    code = re.sub(r'\n?```$', '', code)
    
    return {"playwright_code": code.strip()}
