* def ZoneId = Java.type('java.time.ZoneId')
* def LocalDateTime = Java.type('java.time.LocalDateTime')
* def Formatter = Java.type('java.time.format.DateTimeFormatter')

* def formatter = Formatter.ofPattern("yyyy-MM-dd HH:mm")
* def zone = ZoneId.of("Asia/Singapore")

# Generate dynamic dates
* def policyStartDate = LocalDateTime.now(zone).plusDays(1)
* def lastProductDate = policyStartDate.plusDays(1)
* def lastIssuanceDate = lastProductDate.plusDays(1)

# Convert to formatted strings
* def policyStartDateTime = policyStartDate.format(formatter)
* def lastDateTimeForProductSelection = lastProductDate.format(formatter)
* def lastIssuanceDateTime = lastIssuanceDate.format(formatter)

* print policyStartDateTime
* print lastDateTimeForProductSelection
* print lastIssuanceDateTime
