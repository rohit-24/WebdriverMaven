You are a Senior Playwright Automation Architect.

Analyze ONLY the currently opened helper file.

The framework has already migrated to the grouped `app` fixture. Backward compatibility is no longer required for this helper.

Refactor this helper by removing all legacy page object dependencies and simplifying the constructor.

Current situation:
- The constructor still accepts both `app` and individual page objects.
- `this.app` is initialized using `app ?? { ... }`.
- The helper already uses `app.lots.*`, `app.tiPlus.*`, and `app.release.*` throughout the workflow.

Refactoring requirements:

1. Remove all individual page object constructor parameters:
   - lotsLoginPage
   - lotsDashboardPage
   - lotsImportLetterOfCreditPage
   - tiPlusLoginPage
   - tiPlusDashboardPage
   - tiPlusILCIssuancePage
   - releaseItemsPage
   - and any other page object parameters that are now represented inside `app`.

2. Remove the fallback initialization:

```javascript
this.app = app ?? { ... }
```

Replace it with

```javascript
this.app = app;
```

3. Remove every piece of code that only existed for backward compatibility.

4. Keep using the grouped `app` object directly throughout the helper.

5. Do NOT change business logic.

6. Do NOT rename methods.

7. Do NOT modify Page Objects.

8. Do NOT modify test flow.

9. Remove any unused variables or imports created by the previous migration.

10. Ensure the helper constructor contains only the dependencies that are still required.

Finally, explain:
- what was removed,
- why it is now safe to remove,
- and how the constructor became simpler.
